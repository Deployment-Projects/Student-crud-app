# Datasource default for local dev
#spring.datasource.url=jdbc:postgresql://localhost:5432/studentdb
#spring.datasource.username=postgres
#spring.datasource.password=root

# Below data source code is for docker
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

#Below is for deployment code
#spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/docker-demo-db}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME:lucky}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:papa}

# JPA
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Use dockers at runtime with env override (for docker-compose)
# Example when running with docker-compose these are injected:
# SPRING_DATASOURCE_URL, SPRING_DATASOURCE_USERNAME, SPRING_DATASOURCE_PASSWORD

#This will explicitly tell swagger to use https path
#springdoc.swagger-ui.config-url=/v3/api-docs
#springdoc.swagger-ui.url=/v3/api-docs
#springdoc.api-docs.path=/v3/api-docs

#Below code is for deployment
#springdoc.swagger-ui.disable-swagger-default-url=true
#springdoc.swagger-ui.use-root-path=true
#springdoc.server-url=https://decormoments.shop
#server.forward-headers-strategy=framework