## For Deployment 
1. Replace the url with this
    //for localhost
    // let url = "http://localhost:8080/";

    //for deployment
    let url = "/api/";

2. Create Dockerfile for React
3. Create NGINX config file for React (nginx.conf)